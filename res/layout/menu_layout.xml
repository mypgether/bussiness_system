<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#333333"
    android:paddingBottom="30dp"
    android:paddingLeft="20dp"
    android:paddingTop="15dp" >

    <LinearLayout
        android:id="@+id/menu_user"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/menu_user_logo"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:src="@drawable/app_panel_friend" />

        <TextView
            android:id="@+id/menu_user_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="20dp"
            android:textColor="#ffffff"
            android:textSize="25sp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/menu_bottom_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal" >

        <ImageButton
            android:id="@+id/menu_setting"
            android:layout_width="35dip"
            android:layout_height="35dip"
            android:background="@null"
            android:src="@drawable/button_selector_setting" />

        <TextView
            android:id="@+id/menu_app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="left|center_vertical"
            android:background="@null"
            android:gravity="left|center_vertical"
            android:text="@string/base_menu_setting"
            android:textColor="#ffffff"
            android:textSize="12sp" />
    </LinearLayout>

    <ListView
        android:id="@+id/menu_lv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/menu_bottom_ll"
        android:layout_below="@id/menu_user"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"
        android:cacheColorHint="#00000000"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:textColor="#ffffff" />

</RelativeLayout>